name: together
description: A tool to merge text/code files in a folder
version: 2.10.0
repository: https://github.com/insign/together
executables:
  tog:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  glob: ^2.1.2
  http: ^1.1.0
  path: ^1.9.0
  test: ^1.25.8

dev_dependencies:
  yaml2dart: ^1.5.0
